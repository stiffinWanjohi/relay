apiVersion: v1
kind: Service
metadata:
  name: relay-api
  labels:
    app.kubernetes.io/name: relay
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: relay
    app.kubernetes.io/component: api
